---
// src/components/Services.astro

const services = [
  {
    title: "Diagnóstico Operativo",
    description: "No escribimos una sola línea de código sin entender tu negocio. Analizamos tus cuellos de botella y diseñamos la solución antes de construirla.",
    icon: "🔍"
  },
  {
    title: "Sistematización a la Medida",
    description: "Desarrollamos plataformas que se adaptan a tus reglas de negocio. Desde motores de reserva hasta ERPs internos que eliminan el Excel.",
    icon: "⚙️"
  },
  {
    title: "Infraestructura Escalable",
    description: "Tu sistema crece contigo. Implementamos servidores robustos (VPS/Cloud) que soportan alta demanda sin volverse lentos.",
    icon: "🚀"
  }
];

// Definimos el Stack con sus colores de marca originales
const technologies = [
    { name: "Laravel", color: "text-red-600" },
    { name: "Vue.js", color: "text-green-500" },
    { name: "Astro", color: "text-orange-600" },
    { name: "Tailwind", color: "text-cyan-500" },
    { name: "Linux / VPS", color: "text-yellow-500" } // O gray-800 si prefieres el color de la terminal
];
---

<section id="servicios" class="py-24 bg-lanma-light">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <div class="text-center">
      <h2 class="text-base text-lanma-primary font-semibold tracking-wide uppercase">Nuestra Metodología</h2>
      <p class="mt-2 text-3xl leading-8 font-extrabold text-lanma-dark sm:text-4xl">
        Ingeniería con Enfoque Humano
      </p>
      <p class="mt-4 max-w-2xl text-xl text-gray-500 mx-auto">
        Olvídate de tecnicismos confusos. Nosotros nos encargamos de la complejidad técnica para que tú tengas claridad operativa.
      </p>
    </div>

    <div class="mt-20">
      <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
        {services.map((service) => (
          <div class="pt-6">
            <div class="flow-root bg-white rounded-lg px-6 pb-8 h-full shadow-sm hover:shadow-md transition-shadow duration-300 border border-gray-100">
              <div class="-mt-6">
                <div>
                  <span class="inline-flex items-center justify-center p-3 bg-lanma-primary rounded-md shadow-lg text-2xl">
                    {service.icon}
                  </span>
                </div>
                <h3 class="mt-8 text-lg font-medium text-lanma-dark tracking-tight">{service.title}</h3>
                <p class="mt-5 text-base text-gray-500">
                  {service.description}
                </p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>

    <div class="mt-24 border-t border-gray-200 pt-10">
        <p class="text-center text-sm font-semibold uppercase text-gray-400 tracking-wider mb-8">
            Potenciado por tecnologías modernas
        </p>
        
        <div class="flex flex-wrap justify-center gap-8 md:gap-16 items-center">
            {technologies.map((tech) => (
                <div class={`
                    group flex items-center justify-center 
                    text-2xl font-bold 
                    grayscale hover:grayscale-0 opacity-60 hover:opacity-100 
                    transition-all duration-500 cursor-default hover:scale-110
                    ${tech.color}
                `}>
                    {tech.name}
                </div>
            ))}
        </div>
    </div>

  </div>
</section>